# ── PDF generation ─────────────────────────────────────────────
reportlab>=4.0

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
python-multipart>=0.0.9
httpx>=0.27

# ── LLM ───────────────────────────────────────────────────────
# No SDK needed: agents shell out to the authenticated Claude Code CLI
# (`claude -p`) using the Max-plan session. No API key, no proxy.

# ── Database ──────────────────────────────────────────────────
supabase>=2.6
psycopg[binary]>=3.2

# ── Agents / orchestration ────────────────────────────────────
langgraph>=0.2
langgraph-checkpoint-redis>=0.0.1

# ── Task queue ────────────────────────────────────────────────
celery>=5.4
redis>=5.0

# ── Integrations ──────────────────────────────────────────────
resend>=2.0
google-api-python-client>=2.130
google-auth>=2.30
google-auth-oauthlib>=1.2
apify-client>=1.7

# ── Optional: LinkedIn profile-scraper enrichment (OPT-IN, heavy, ToS risk) ──
# Not installed by default. To enable, run:
#   pip install linkedin-scraper playwright && playwright install chromium
# linkedin-scraper>=3.1
# playwright>=1.40
