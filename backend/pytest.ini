[pytest]
# Run from backend/. pythonpath=. makes the top-level packages (services,
# agents, models, db, routers) importable the same way the app imports them.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
